spring:
  cloud:
    nacos:
      config:
        server-addr: my.service.com:8848
        file-extension: yaml
  application:
    name: gateway-config
  main:
    allow-bean-definition-overriding: true
